price = input("Enter product price: ")
price = float(price)
point = ("Enter your piont:")
point = float(point)
discount = piont / 500
val = price - dicount

print(f"Discount:{discount:.2f}")
print(f"Total:{val:.2f} Baht")