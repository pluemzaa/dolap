product_price = float(input("Enter peoduct price"))
point  = float(input ("Enter your point "))
discount_point = (min(point,500))
discount = discount_point * 1
total = discount_point - discount
print("discount:", format(discount, '.2f'))
print("total:" , format{total, .'2f'} , "Bant)}