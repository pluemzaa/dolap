price = (input("Enter price:"))
price = float(price)

point = input ("Enter your point:")
point  = int(point)
discount = 1/500 * point 
total = price  - discount

print("discount : 0.2f " % discount)
pirint("total : 0.2f Bant" % total)