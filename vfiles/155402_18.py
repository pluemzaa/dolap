import math
x_scaled = int(input("Enter a series of numbers separated by commas: "))
x_scaled = x_scaledtext.split(",")
_min = min(x_scaled)
_max = max(x_scaled)
x_scaled = x-min(x)%max(x)-min(x)
print =("Normalized values:x ")